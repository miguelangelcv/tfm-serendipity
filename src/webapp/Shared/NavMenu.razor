<div class="top-row pl-4 navbar navbar-dark">
    <img src="images/SerendipityLogoLite.png" height="32" />
    <button class="navbar-toggler" @onclick="ToggleNavMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <ul class="nav flex-column">
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </li>
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="playlists">
                <span class="oi oi-musical-note" aria-hidden="true"></span> Mis playlists
            </NavLink>
        </li>
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="getlucky">
                <span class="oi oi-star" aria-hidden="true"></span> ¡Voy a tener suerte!
            </NavLink>
        </li>
    </ul>
</div>

@code {
    private bool collapseNavMenu = true;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
